<Window x:Class="RandPicker.WPF.Views.StudentManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewmodels="clr-namespace:RandPicker.WPF.ViewModels"
        xmlns:converters="clr-namespace:RandPicker.WPF.Converters"
        mc:Ignorable="d"
        Title="学生管理" 
        Height="600" 
        Width="800"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        d:DataContext="{d:DesignInstance Type=viewmodels:StudentManagementViewModel}">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
        <converters:BooleanToVisibilityInverseConverter x:Key="BooleanToVisibilityInverseConverter" />
    </Window.Resources>
    
    <materialDesign:DialogHost Identifier="StudentManagementDialog">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题和工具栏 -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="学生管理" 
                           Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Command="{Binding ImportStudentsCommand}"
                            ToolTip="导入学生"
                            Style="{StaticResource MaterialDesignIconButton}">
                        <materialDesign:PackIcon Kind="Import" Width="24" Height="24"/>
                    </Button>
                    <materialDesign:PopupBox Style="{StaticResource MaterialDesignPopupBox}" PlacementMode="BottomAndAlignRightEdges">
                        <StackPanel>
                            <TextBlock Text="导出" Margin="16,8" Style="{StaticResource MaterialDesignSubtitle1TextBlock}"/>
                            <Separator/>
                            <Button Content="导出为Excel" Command="{Binding ExportStudentsCommand}" CommandParameter="excel"/>
                            <Button Content="导出为CSV" Command="{Binding ExportStudentsCommand}" CommandParameter="csv"/>
                            <Button Content="导出为PDF" Command="{Binding ExportStudentsCommand}" CommandParameter="pdf"/>
                            <Button Content="导出为JSON" Command="{Binding ExportStudentsCommand}" CommandParameter="json"/>
                        </StackPanel>
                    </materialDesign:PopupBox>
                    <materialDesign:PopupBox Style="{StaticResource MaterialDesignPopupBox}" PlacementMode="BottomAndAlignRightEdges">
                        <StackPanel>
                            <TextBlock Text="批量操作" Margin="16,8" Style="{StaticResource MaterialDesignSubtitle1TextBlock}"/>
                            <Separator/>
                            <Button Content="重置权重" Command="{Binding ResetWeightsCommand}"/>
                            <Button Content="全部启用" Command="{Binding ResetActiveCommand}" CommandParameter="true"/>
                            <Button Content="全部禁用" Command="{Binding ResetActiveCommand}" CommandParameter="false"/>
                        </StackPanel>
                    </materialDesign:PopupBox>
                    <Button Command="{Binding AddStudentCommand}"
                            ToolTip="添加学生"
                            Style="{StaticResource MaterialDesignIconButton}">
                        <materialDesign:PackIcon Kind="AccountPlus" Width="24" Height="24"/>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- 主内容区域 -->
            <Grid Grid.Row="1" Margin="0,16,0,0">
                <!-- 学生列表 -->
                <Grid Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityInverseConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 搜索框 -->
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="搜索学生..."
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,0,16">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Return" Command="{Binding SearchCommand}"/>
                        </TextBox.InputBindings>
                        <TextBox.TrailingIcon>
                            <Button Command="{Binding SearchCommand}"
                                    Style="{StaticResource MaterialDesignIconButton}">
                                <materialDesign:PackIcon Kind="Magnify"/>
                            </Button>
                        </TextBox.TrailingIcon>
                    </TextBox>

                    <!-- 学生数据表格 -->
                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding FilteredStudents}"
                              SelectedItem="{Binding SelectedStudent}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              Style="{StaticResource MaterialDesignDataGrid}">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="启用" 
                                                    Binding="{Binding Active, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                    Width="60"/>
                            <DataGridTextColumn Header="姓名" 
                                                Binding="{Binding Name}" 
                                                Width="*"/>
                            <DataGridTextColumn Header="学号" 
                                                Binding="{Binding Id}" 
                                                Width="100"/>
                            <DataGridTextColumn Header="权重" 
                                                Binding="{Binding Weight}" 
                                                Width="80"/>
                            <DataGridTemplateColumn Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Command="{Binding DataContext.EditStudentCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    ToolTip="编辑"
                                                    Style="{StaticResource MaterialDesignIconButton}">
                                                <materialDesign:PackIcon Kind="Pencil"/>
                                            </Button>
                                            <Button Command="{Binding DataContext.DeleteStudentCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    ToolTip="删除"
                                                    Style="{StaticResource MaterialDesignIconButton}">
                                                <materialDesign:PackIcon Kind="Delete"/>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <!-- 编辑表单 -->
                <materialDesign:Card Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}"
                                     Padding="16"
                                     VerticalAlignment="Center"
                                     HorizontalAlignment="Center"
                                     Width="400">
                    <StackPanel>
                        <TextBlock Text="编辑学生" 
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   Margin="0,0,0,16"/>

                        <TextBox Text="{Binding NewStudent.Name}"
                                 materialDesign:HintAssist.Hint="姓名"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Margin="0,0,0,16"/>

                        <TextBox Text="{Binding NewStudent.Id}"
                                 materialDesign:HintAssist.Hint="学号"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Margin="0,0,0,16"/>

                        <TextBox Text="{Binding NewStudent.Weight}"
                                 materialDesign:HintAssist.Hint="权重"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Margin="0,0,0,16"/>

                        <CheckBox Content="启用"
                                  IsChecked="{Binding NewStudent.Active}"
                                  Style="{StaticResource MaterialDesignCheckBox}"
                                  Margin="0,0,0,16"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="取消" 
                                    Command="{Binding CancelEditCommand}"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Margin="0,0,8,0"/>
                            <Button Content="保存" 
                                    Command="{Binding SaveStudentCommand}"
                                    Style="{StaticResource MaterialDesignRaisedButton}"/>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>

                <!-- 加载遮罩 -->
                <Grid Background="#80000000" 
                      Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ProgressBar IsIndeterminate="True" 
                                 Style="{StaticResource MaterialDesignCircularProgressBar}"
                                 Value="0"
                                 Width="48"
                                 Height="48"/>
                </Grid>
            </Grid>
        </Grid>
    </materialDesign:DialogHost>
</Window>